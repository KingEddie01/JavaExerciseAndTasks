package tdd;

public class Reversal {
    public static String reverse(String...numbers){
        int[] reverse = new int[numbers.length];
        int newLength = numbers.length - 1;
        int newReverse = 0;
        for (int counter = newLength; counter > 0; counter--){
            newReverse = reverse[counter];
        }

return null;
    }








}
